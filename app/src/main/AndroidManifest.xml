<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sangameswaran.nammachennai">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>
        <activity android:name=".AddEventActivity"></activity>
        <activity android:name=".FitnessRecyclerViewActivity"></activity>
        <activity android:name=".EntertainmentRecyclerViewActivity"></activity>
        <activity android:name=".TechnicalRecyclerViewActivity"></activity>
        <activity android:name=".AwarenessRecyclerView"></activity>
        <activity android:name=".MapsActivity"></activity>
        <activity android:name=".ViewEventAuthorizer"></activity>
        <activity android:name=".AddBookActivity"></activity>
        <activity android:name=".ViewBookActivity"></activity>
        <activity android:name=".ManageBookActivity"></activity>

    </application>

</manifest>